//changes made in previous code

// in in SimpleAnimtion class
Implement a nested class Build.
add a new field canvas which is a Rectangle.
add a new method getBox() which return the value of canvas.
add a new field contact which is HashMap<String, Shpae> inorder to store the information for each
shape and its type.
 change the signature of declareShape method from "public void declareShape(String name)"  to
 "public void declareShape(String name, String type) in order to adjust to method in Build.

 // new Implementation

 add a ShapeCreator class in cs3500.animator.shape
 add a View