//changes made in previous code

// in in SimpleAnimtion class
Implement a nested class Build.
added a new field canvas which is a Rectangle.
added a new method getBox() which return the value of canvas.
added a new field contact which is HashMap<String, Shpae> inorder to store the information for each
shape and its type.
 changed the signature of declareShape method from "public void declareShape(String name)"  to
 "public void declareShape(String name, String type) in order to adjust to method in Build and modify
 its code.
 Modified addMotion();

 // new Implementation

 add a ShapeCreator class in cs3500.animator.shape
 add a View