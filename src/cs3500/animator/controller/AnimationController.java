package cs3500.animator.controller;

/**
 * controllers for the Animation that will run the display of the view.
 */
public interface AnimationController {

  /**
   * execute the view to generate output.
   */
  void execute();
}
